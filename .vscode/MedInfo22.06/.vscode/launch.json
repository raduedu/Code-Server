{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build active file (g++)"
    }
  ]
}
